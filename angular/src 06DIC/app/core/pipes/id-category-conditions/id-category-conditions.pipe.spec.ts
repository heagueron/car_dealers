import { IdCategoryConditionsPipe } from './id-category-conditions.pipe';

describe('IdCategoryConditionsPipe', () => {
  it('create an instance', () => {
    const pipe = new IdCategoryConditionsPipe();
    expect(pipe).toBeTruthy();
  });
});
